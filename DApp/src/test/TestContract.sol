// SPDX-License-Identifier: MIT
pragma solidity >=0.4.21 <0.7.0;


import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/PrinterMarketplace.sol";

contract TestPrinterMarketplace {
/*     PrinterMarketplace offerquotation = PrinterMarketplace(
        DeployedAddresses.PrinterMarketplace()
    );
    uint expectedOfferPrice = 100;
    uint returnedPrice;

    //address expectedProvider = address(this);

    function testUserCanWriteOfferPrice() public {
        returnedPrice = offerquotation.setOfferPrice(expectedOfferPrice);

        Assert.equal(returnedPrice, expectedOfferPrice, "Price is different");
    } */

    string hash;

}
